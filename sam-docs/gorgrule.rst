.gorgrule File
=====================

The :code:`.gorgrule` file allows a directory to have rules
that need to be followed by it's contained assets. For example,
it will restrict the type of tags that are allowed, or impose
a custom field, in order to ensure uniformity accross assets.